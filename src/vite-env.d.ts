/// <reference types="vite/client" />

// Injected at build time by Vite define (vite.config.ts)
// Value comes from package.json `version` field via npm_package_version
declare const __APP_VERSION__: string;
