# Architecture Decision Log

| Date | Decision | Context |
|------|----------|---------|
| 2026-04-13 | Register `customer-portal` and `request-plan` in `supabase/config.toml` (PR#02) | Both functions existed in `supabase/functions/` but were missing from config — made registration explicit for deployment consistency |
| 2026-04-13 | SEC-01: public offer approval access via SECURITY DEFINER RPC (Option B) — `get_offer_approval_by_token()` — not RLS anon policy | Broken USING clause on anon policy passed row's own `public_token` to `validate_offer_token()`, always returning TRUE → full enumeration of all pending offers. RLS cannot safely read the caller's filter predicate. SECURITY DEFINER function performs exact token match server-side, returns minimal fields only, revokes anon access to raw table. |
| 2026-04-13 | PR-ARCH-02: Remove dead legacy hooks (useOfferApprovals.ts — 5 functions + OfferTrackingTimeline.tsx) + publish docs/COMPATIBILITY_MATRIX.md | Dead-code audit after PR-ARCH-01 confirmed zero callers for all 5 functions in useOfferApprovals (useOfferApprovals, useCreateOfferApproval, useExtendOfferApproval, usePublicOfferApproval, useSubmitOfferApproval) and zero import callers for OfferTrackingTimeline. Removed safely — no customer-facing route affected. No feature flag added: rollout risk is trivial (pure dead-code deletion, zero behavior change). Feature flag decision: existing flags (FF_NEW_SHELL, FF_MODE_B_DOCX_ENABLED etc.) are UI/shell flags — none applicable to offer flow consolidation. Legacy routes /offer/:token and /oferta/:token retained. Next: PR-ARCH-03 fills L-1/L-2 gaps. |
