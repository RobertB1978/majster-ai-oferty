# Architecture Decision Log

| Date | Decision | Context |
|------|----------|---------|
| 2026-04-14 | PR-INFRA-01: canonical production domain fixed to `https://majsterai.com` — removed `VERCEL_URL` preview-domain leakage from sitemap generator; aligned `SEOHead` canonical/og:url to use env-sourced origin (`siteUrl`) instead of `window.location.origin` | `VERCEL_URL` is auto-injected by Vercel on every build (including preview deployments), causing `sitemap.xml` and canonical tags to be generated with the preview hostname. Removed `VERCEL_URL`/`URL` from `getBaseUrl()` fallback chain. Fixed `SEOHead.tsx` canonical construction to prefer `siteUrl` (from `VITE_PUBLIC_SITE_URL`) over `window.location.origin`. Updated `.env.example` default from stale `vercel.app` URL to `https://majsterai.com`. |
| 2026-04-13 | Register `customer-portal` and `request-plan` in `supabase/config.toml` (PR#02) | Both functions existed in `supabase/functions/` but were missing from config — made registration explicit for deployment consistency |
| 2026-04-13 | SEC-01: public offer approval access via SECURITY DEFINER RPC (Option B) — `get_offer_approval_by_token()` — not RLS anon policy | Broken USING clause on anon policy passed row's own `public_token` to `validate_offer_token()`, always returning TRUE → full enumeration of all pending offers. RLS cannot safely read the caller's filter predicate. SECURITY DEFINER function performs exact token match server-side, returns minimal fields only, revokes anon access to raw table. |
| 2026-04-13 | PR-ARCH-02: Remove dead legacy hooks (useOfferApprovals.ts — 5 functions + OfferTrackingTimeline.tsx) + publish docs/COMPATIBILITY_MATRIX.md | Dead-code audit after PR-ARCH-01 confirmed zero callers for all 5 functions in useOfferApprovals (useOfferApprovals, useCreateOfferApproval, useExtendOfferApproval, usePublicOfferApproval, useSubmitOfferApproval) and zero import callers for OfferTrackingTimeline. Removed safely — no customer-facing route affected. No feature flag added: rollout risk is trivial (pure dead-code deletion, zero behavior change). Feature flag decision: existing flags (FF_NEW_SHELL, FF_MODE_B_DOCX_ENABLED etc.) are UI/shell flags — none applicable to offer flow consolidation. Legacy routes /offer/:token and /oferta/:token retained. Next: PR-ARCH-03 fills L-1/L-2 gaps. Baseline ADR: ADR-0014 (przemianowane z błędnego ADR-0005 w PR-OPS-01). |
| 2026-04-14 | PR-OPS-01: Phantom hash `5669817` — brak jakichkolwiek trafień w repo; referencja nigdy nie istniała | Pełne przeszukanie repo (grep wszystkich plików + git log --all) dla stringa `5669817` — zero wyników. Hash nie istnieje ani w plikach, ani w historii git. Jest to fałszywa referencja nigdy nie zapisana w kodzie ani dokumentacji. Żadna zmiana kodu nie jest wymagana. Decyzja: udokumentować jako audyt negatywny i zakończyć — brak obiektów do usunięcia. |
| 2026-04-14 | PR-OPS-01: Naprawiono konflikt numeracji ADR — `docs/ADR-0005-public-offer-canonical-flow.md` przemianowane na `docs/ADR/ADR-0014-public-offer-canonical-flow.md` | Plik `docs/ADR-0005-public-offer-canonical-flow.md` (2026-04-13, PR-ARCH-01) używał numeru ADR-0005, który był już zajęty przez `docs/ADR/ADR-0005-shell-feature-flag.md` (2026-03-01, PR-07). Kolizja powodowała niejednoznaczność we wszystkich odwołaniach do „ADR-0005". Plik przeniesiony do katalogu `docs/ADR/` z numerem ADR-0014 (następny wolny po ADR-0013). Zaktualizowano `COMPATIBILITY_MATRIX.md` i `DECISIONS.md`. |
| 2026-04-14 | PR-DOCS-01: Wiring `FF_MODE_B_DOCX_ENABLED` w `ReadyDocuments.tsx` — flaga teraz faktycznie gatuje ścieżkę Mode B | Audyt wykazał, że `FF_MODE_B_DOCX_ENABLED` (default=false, PR-01) istniała w `featureFlags.ts` ale NIE była importowana ani sprawdzana w żadnym pliku UI — flaga była martwym kodem. Gap: `ReadyDocuments` renderował pełny UI Mode B niezależnie od wartości flagi. Naprawa (minimalna): zaimportowano `FF_MODE_B_DOCX_ENABLED` w `ReadyDocuments.tsx:67` i dodano guard po hookach (linia ~476) — gdy flaga=false, strona renderuje dormant placeholder używając istniejących kluczy i18n `readyDocs.emptyState.*`. Tryb A (PDF, DocumentTemplates, jsPDF) jest nienaruszony — brak jakichkolwiek referencji Mode B w plikach Mode A. Co ten PR celowo NIE robi: nie wdraża Edge Function generatora DOCX, nie implementuje uploadu pliku DOCX przez użytkownika, nie zmienia domyślnej ścieżki użytkownika w Trybie A. Następny PR (PR-DOCS-02 / PR-02 wg ADR-0013): pilot techniczny DOCX — Edge Function `generate-docx-mode-b`, instalacja `docxtemplater`+`pizzip`, test end-to-end z dokumentem „Protokół odbioru końcowego", weryfikacja kryteriów S1–S10 z ADR-0013, jawna decyzja GO/NO-GO przed PR-03+. Włączenie Mode B w PR-02: `VITE_FF_MODE_B_DOCX_ENABLED=true` (build) lub `localStorage.setItem('FF_MODE_B_DOCX_ENABLED', 'true')` (dev). |
